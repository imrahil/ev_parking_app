# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

The repository's agent instructions live in `AGENTS.md` (shared across coding
agents). Read it — commands, architecture, and the constraints that aren't
obvious from the code are all there.

@AGENTS.md
